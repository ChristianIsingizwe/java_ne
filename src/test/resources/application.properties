# Test profile: use an in-memory H2 database (PostgreSQL-compatible mode) so the
# context loads without a live PostgreSQL/SMTP server. Flyway runs the same
# migrations against H2.
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=validate

spring.flyway.enabled=true

app.mail.enabled=false

app.base-url=http://localhost:8080
app.mail.from=Java Exam <no-reply@java-exam.local>
app.jwt.secret=Yk9wQ2x2N3pHc1J0VWlPcEFzRGZHaEprTHpYY1ZiTm1Rd0VyVHk=
app.jwt.access-expiration-ms=900000
app.jwt.refresh-expiration-ms=604800000
app.verification.expiration-minutes=1440
app.password-reset.expiration-minutes=60
app.cleanup.interval-ms=3600000
app.admin.email=admin@java-exam.local
app.admin.password=admin12345
app.admin.first-name=System
app.admin.last-name=Administrator
